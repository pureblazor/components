<pre class="my-3 text-sm px-2 pt-3 pb-2 leading-normal rounded border-1 border-gray-200 bg-gray-50">
            <code>
                @Loader()
            </code>
        </pre>

@code {
    [Parameter] public required Func<MarkupString> Loader { get; set; }
}
